<%= form_for(@wowolink) do |f| %>
  <% if @wowolink.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wowolink.errors.count, "error") %> prohibited this wowolink from being saved:</h2>

      <ul>
      <% @wowolink.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
